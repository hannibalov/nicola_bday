/** Wall time for a music bingo round; host/music stops and scores finalize when this elapses. */
export const BINGO_ROUND_DURATION_MS = 20 * 60 * 1000;

/** Points lost when a player marks a tile that is not the song currently playing (verified on server). */
export const BINGO_WRONG_TAP_PENALTY = 5;
